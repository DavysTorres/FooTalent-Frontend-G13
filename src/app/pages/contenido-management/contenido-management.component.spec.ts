/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ContenidoManagementComponent } from './contenido-management.component';

describe('ContenidoManagementComponent', () => {
  let component: ContenidoManagementComponent;
  let fixture: ComponentFixture<ContenidoManagementComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ContenidoManagementComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ContenidoManagementComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/